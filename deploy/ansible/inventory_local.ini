# Converging the box in place, from a shell on the box itself -- the common
# case, since that is where the repo checkout and the runner already live.
# Same host as inventory.ini, reached without SSH: the `headless` alias exists
# only in a workstation's ~/.ssh/config and does not resolve here.
[box]
headless ansible_connection=local
