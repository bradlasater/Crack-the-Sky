# Converging the box from a workstation. `headless` is an SSH alias out of
# ~/.ssh/config, so it resolves only where that config lives -- NOT on the box
# itself, which has no such alias and fails with "Could not resolve hostname
# headless". To converge the box in place, use inventory_local.ini instead.
[box]
headless
