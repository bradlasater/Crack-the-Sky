.env
.DS_Store
data/
logs/
venv/
.ansible-venv/
__pycache__/
*.pyc
*.jsonl.gz
*.egg-info/
venv
.env.example.bak

# Claude Code scratch space: agent worktrees and session state. Untracked and
# ignored on purpose -- a worktree here is a whole second checkout, and on a
# public repo one `git add -A` would publish it.
.claude/
